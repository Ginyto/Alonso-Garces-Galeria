{
	"scripts": {
		"start": "npx http-server -P http://127.0.0.1:${PORT}/home.html",
		"dev": "nodemon --watch src --exec \"npm start\""
	},
	"dependencies": {
		"http-server": "^14.1.1",
		"nodemon": "^2.0.20"
	}
}
